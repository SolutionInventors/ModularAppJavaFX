package utils;

/**
 * This enum is used to select a type of student data. It should used when updating
 * specific  information about a {@code Student}. For example if we want to update
 * a specific type of {@code StudentData} then this class should be used
 * 
 * @author Oguejiofor Chidiebere
 *
 */
public enum StudentInfo
{
    BIODATA, MEANS_OF_DISCOVERY, PHONE_NUMBER, PROFESSIONAL_EXPERIENCE,
    ALL;
}
